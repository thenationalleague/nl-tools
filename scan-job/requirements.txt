# Pinned, because SIFT's output is the measurement. A minor OpenCV bump can
# change how many keypoints a frame yields, which moves every number this tool
# reports — and it would move them silently, on a rebuild nobody thought of as
# a change. The engine version in the profile string is meant to be the thing
# that tells you two matches are not comparable; it cannot do that if the
# library underneath moves on its own.
#
# Matches what the local runs were measured with. Bump both together, and
# expect the profile string to change with them.
opencv-python-headless==5.0.0.93
numpy==2.4.6

# Nothing else. board_exposure_upload.py is deliberately urllib-only, so there
# is no requests, no firebase-admin and no google-cloud-storage in this image —
# the job authenticates exactly as the laptop does, with an ingest key, and gets
# a token scoped to the one match it was told to measure. A service-account key
# in here would be a credential that could write every match in the tool.
