{
  "name": "nl-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && rm -rf ../site-demo && cp -r dist ../site-demo",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "embla-carousel": "^8.6.0",
    "piccolore": "^0.1.3"
  }
}
